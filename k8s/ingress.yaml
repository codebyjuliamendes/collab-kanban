apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: collab-kanban-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: collab-kanban-service
            port:
              number: 3002
